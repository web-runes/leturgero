---
"@leturgero/cli": minor
---

Adds optimized fallbacks

Metric-adjusted `@font-face` rules built from system fonts that reduce layout shift (CLS) while your font loads can now be generated using the CLI
